# 04. Методы и параметры

## Оглавление
- [[#Объявление методов|Объявление методов]]
- [[#Параметры и аргументы|Параметры и аргументы]]
- [[#Возвращение значений|Возвращение значений]]
- [[#Перегрузка методов|Перегрузка методов]]
- [[#Аргументы переменной длины|Аргументы переменной длины]]
- [[#Рекурсия|Рекурсия]]
- [[#Статические методы|Статические методы]]
- [[#Передача поведения (функциональные параметры)|Передача поведения (функциональные параметры)]]
- [[#Аргументы командной строки|Аргументы командной строки]]
- [[#Ковариантные возвращаемые типы|Ковариантные возвращаемые типы]]

## Объявление методов

Общая форма:

```java
модификаторы типВозврата имя(параметры) {
    // тело
}
```

Пример:

```java
public int add(int a, int b) {
    return a + b;
}
```

### Тип возвращаемого значения

- Примитив, ссылочный тип или `void` (ничего не возвращает).
- `void`-метод может содержать `return;` для раннего выхода.

### Имя

- Идентификатор по правилам именования (см. [[01. Основы языка Java#Идентификаторы и ключевые слова|идентификаторы]]).
- Соглашение — lowerCamelCase: `getTotal`, `printReport`.

### Параметры

Список переменных, принимаемых методом: `тип имя`. Параметры — это локальные переменные метода.

В Java **нет** значений параметров по умолчанию и именованных аргументов (в отличие от Python/Kotlin) — аргументы передаются строго в порядке объявления. Замена отсутствию дефолтов — перегрузка (см. [[#Перегрузка методов|перегрузку]]):

```java
// вместо default-параметра
void greet(String name, int times) { ... }
void greet(String name) { greet(name, 1); }   // перегрузка как «default»
```

### Модификаторы методов

| Модификатор | Назначение |
| --- | --- |
| `public` / `protected` / `private` | доступ (см. [[05. Классы и объекты#Управление доступом|управление доступом]]) |
| `static` | принадлежит классу, не экземпляру (см. [[#Статические методы|статические методы]]) |
| `final` | запрет переопределения (см. [[06. Наследование и полиморфизм#final и наследование|final и наследование]]) |
| `abstract` | без тела, требует реализации в подклассе (см. [[06. Наследование и полиморфизм#Абстрактные классы|абстрактные классы]]) |
| `synchronized` | блокировка монитора при вызове (см. [[14. Многопоточность и конкурентность#Синхронизация|синхронизацию]]) |
| `native` | реализация на другом языке, тело отсутствует |

### `final`-параметры

Параметр `final` нельзя переприсвоить внутри метода:

```java
void process(final int x) {
    // x = 10;  // ошибка: final-параметр
}
```

На практике используется редко, но встречается в старом коде.

### Спецификатор `strictfp`

Гарантирует одинаковые результаты вещественных операций на всех платформах (отключает расширенную точность x87):

```java
strictfp void calculate() {
    double a = 0.1, b = 0.2;
    double c = a + b;   // одинаково на всех платформах
}
```

С Java 17 `strictfp` применяется по умолчанию ко всем вещественным операциям.

### Возврат нескольких значений

Метод возвращает только одно значение. Для нескольких значений — record, класс или массив:

```java
record MinMax(int min, int max) { }

MinMax findMinMax(int[] arr) {
    int min = arr[0], max = arr[0];
    // ... поиск
    return new MinMax(min, max);
}
```

### Тело метода и `return`

`return` завершает метод и возвращает значение. Для не-`void` методов обязательно наличие `return` на всех путях выполнения.

```java
int max(int a, int b) {
    if (a > b) return a;
    return b;
}
```

В `void`-методе `return;` завершает выполнение досрочно — основа guard clauses (ранний выход вместо вложенных `if`, см. [[02. Операторы и управление потоком#Вложенные условия|guard clauses]]):

```java
void sendEmail(User u) {
    if (u == null) return;       // ранний выход
    if (u.email() == null) return;
    // основной код
}
```

## Параметры и аргументы

**Параметр** — переменная в объявлении. **Аргумент** — фактическое значение при вызове.

### Примитивные аргументы

Передаётся копия значения — изменение параметра не влияет на вызывающий код.

```java
void inc(int x) { x++; }

int v = 5;
inc(v);
// v == 5 — не изменилось
```

### Ссылочные аргументы

Передаётся копия ссылки на тот же объект.

### Передача по значению

Java всегда передаёт параметры **по значению**: для примитивов — значение, для ссылочных типов — значение ссылки (адрес объекта). Сам объект не копируется.

```java
void change(StringBuilder sb) {
    sb.append("!");        // меняет объект — видно снаружи
    sb = new StringBuilder("new"); // переприсваивание — НЕ видно снаружи
}

StringBuilder b = new StringBuilder("hi");
change(b);
// b == "hi!"
```

### Изменение объектов внутри метода

Изменение состояния переданного объекта видно вызывающему коду (через ссылку), а переприсваивание параметра — нет.

## Возвращение значений

### Примитивы, объекты, массивы, `null`

```java
int answer() { return 42; }
String name() { return "Java"; }
int[] data() { return new int[]{1, 2}; }
String maybe() { return null; }   // ссылочный тип может вернуть null
```

При возврате ссылочного типа возвращается ссылка, объект не копируется.

## Перегрузка методов

Методы с одинаковым именем, но разными сигнатурами (параметрами). Позволяет одинаково называть похожие операции.

### Разные количества параметров

```java
int sum(int a, int b) { return a + b; }
int sum(int a, int b, int c) { return a + b + c; }
```

### Разные типы параметров

```java
double convert(double v) { return v; }
String convert(String s) { return s; }
```

### Разный порядок типов

```java
void log(String msg, int level) { }
void log(int level, String msg) { }
```

Сигнатуру определяют типы и их порядок, **не** имена параметров и не тип возврата:

```java
// void f() { }      // и
// int f() { }       // — одинаковые сигнатуры, так нельзя
```

### Перегрузка и generics

Из-за стирания типов (см. [[09. Обобщения и система типов#Type erasure|type erasure]]) перегрузка, различающаяся только generic-параметром, не компилируется:

```java
// void print(List<String> l) { }   // и
// void print(List<Integer> l) { }  // — конфликт: после стирания обе List
```

Различать нужно типом самого параметра: `List<String>` vs `String` — допустимо.

### Выбор перегруженного метода

Компилятор выбирает наиболее точное соответствие аргументов.

### Автоматические преобразования

Если точного совпадения нет, подбирается метод через неявное расширение типов (см. [[01. Основы языка Java#Неявное расширение|расширение типов]]).

```java
void print(int x) { }
print(42L);   // ошибка: long не сужается автоматически
print(42);    // ок
```

### Неоднозначность

Если аргумент подходит двум методам одинаково, компилятор выдаёт ошибку.

```java
void f(int a, double b) { }
void f(double a, int b) { }
// f(1, 1);  // ошибка: неоднозначный вызов
```

## Аргументы переменной длины

### Синтаксис и использование

Varargs позволяют передавать произвольное число аргументов одного типа. Синтаксис — три точки.

```java
int sum(int... values) {
    int s = 0;
    for (int v : values) s += v;
    return s;
}

sum()            // 0
sum(1)           // 1
sum(1, 2, 3)     // 6
```

### Varargs как массив

Внутри метода varargs — это массив. Вызов с массивом тоже допустим.

```java
int[] arr = {1, 2, 3};
sum(arr);    // 6
```

**Осторожно с `null`**: `sum(null)` неоднозначен (компилятор может принять его как массив `int[]`), а разворачивание пустых varargs даёт пустой массив — не `null`.

### Ограничения

Varargs-параметр должен быть последним; в методе — только один.

```java
void f(String prefix, int... values) { }   // ок
// void g(int... a, String s) { }          // ошибка: не последний
```

### Перегрузка и неоднозначность

Перегрузка с varargs может быть неоднозначной:

```java
void p(int... a) { }
void p(int a, int... b) { }
// p(1); // ошибка: обе подходят
```

## Рекурсия

Метод, вызывающий сам себя. Решает задачи с самоподобной структурой (обход деревьев, факториал, Фибоначчи).

### Базовый случай

Условие остановки — без него рекурсия бесконечна.

### Рекурсивный вызов

Вызов с аргументами, приближающими к базовому случаю.

```java
int factorial(int n) {
    if (n <= 1) return 1;      // базовый случай
    return n * factorial(n - 1); // рекурсивный вызов
}
```

### Стек вызовов

Каждый вызов кладёт кадр в стек (см. [[15. JVM, память и профессиональная разработка#Память JVM|стек JVM]]): локальные переменные и адрес возврата. Глубина рекурсии ограничена размером стека.

### Рекурсия против итерации

| | Рекурсия | Итерация |
| --- | --- | --- |
| Читаемость | лучше для деревьев/вложенности | лучше для простых циклов |
| Память | расходует стек | константная |
| Риски | `StackOverflowError` при большой глубине | нет |

### Хвостовая рекурсия не оптимизируется

JVM **не** выполняет оптимизацию хвостовой рекурсии (TCO), в отличие от многих функциональных языков. Даже если рекурсивный вызов — последняя операция, стек всё равно растёт. Глубокие рекурсии заменять итерацией или Stream (см. [[11. Лямбда-выражения и Stream API|Stream API]]).

### `StackOverflowError`

При слишком глубокой рекурсии переполняется стек — выбрасывается `StackOverflowError` (это `Error`, см. [[08. Исключения и обработка ошибок#Иерархия исключений|иерархию исключений]]).

```java
// бесконечная рекурсия -> StackOverflowError
int infinite(int n) { return infinite(n + 1); }
```

## Статические методы

Принадлежат классу, а не экземпляру. Вызываются через имя класса (см. [[05. Классы и объекты#Ключевое слово static|static]]).

### Доступ к статическим членам

```java
class MathUtils {
    static int square(int x) { return x * x; }
}

int s = MathUtils.square(5);   // 25
```

### Ограничения статического контекста

Статический метод не может обращаться к нестатическим полям/методам (у него нет экземпляра) и не может использовать `this`.

```java
class Demo {
    int value;
    static void bad() {
        // value = 5;   // ошибка: нет экземпляра
        // this.value;  // ошибка: this недоступен
    }
}
```

### `this` и `static`

`this` — ссылка на текущий экземпляр; в статическом контексте её нет. Статические методы вызываются без объекта.

## Передача поведения (функциональные параметры)

Методы принимают не только данные, но и **поведение** — через функциональные интерфейсы и лямбды (см. [[11. Лямбда-выражения и Stream API#Функциональные интерфейсы|функциональные интерфейсы]]):

```java
void forEach(int[] arr, IntConsumer action) {
    for (int v : arr) action.accept(v);
}

forEach(new int[]{1, 2, 3}, v -> System.out.println(v * 2));   // 2 4 6
```

Это основа стратегии «передать правило, а не писать ветки» — `Predicate`/`Function`/`Consumer`/`Supplier` как параметры (ср. `removeIf`, `sort(Comparator)` в [[10. Коллекции и структуры данных|коллекциях]]).

## Аргументы командной строки

### `String[] args`

Метод `main` принимает аргументы командной строки как массив строк (см. [[01. Основы языка Java#Метод main|метод main]]).

```text
java App alpha 42
```

### Разбор и проверка количества

```java
public static void main(String[] args) {
    if (args.length < 2) {
        System.out.println("Использование: App <имя> <возраст>");
        return;
    }
    String name = args[0];
    int age = Integer.parseInt(args[1]);
    System.out.println(name + ": " + age);
}
```

### Преобразование строк в числа

Парсинг через wrapper-классы (см. [[13. Дата, время, числа и полезные API#Парсинг чисел|парсинг чисел]]); некорректное число бросает `NumberFormatException`.

```java
int n = Integer.parseInt("42");
double d = Double.parseDouble("3.14");
long l = Long.parseLong("1234567890");
```

## Ковариантные возвращаемые типы

Подкласс может сузить возвращаемый тип переопределённого метода — это называется ковариантным возвратом (см. [[06. Наследование и полиморфизм#Ковариантные возвращаемые типы|наследование]]). Позволяет избежать приведения:

```java
class Animal {
    Animal copy() { return new Animal(); }
}
class Dog extends Animal {
    @Override
    Dog copy() { return new Dog(); }   // ковариантный возврат
}

Dog d = new Dog();
Dog clone = d.copy();   // без приведения (Dog, а не Animal)
```

Ковариантный возврат возможен для ссылочных типов; примитивные типы сужать нельзя.
